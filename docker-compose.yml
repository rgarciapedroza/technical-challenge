services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: internal_requests
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d internal_requests"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
